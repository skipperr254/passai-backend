# Supabase Configuration (Required)
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_service_role_key

# OpenAI Configuration (Required if using OpenAI as embedding provider)
OPENAI_API_KEY=your_openai_api_key

# Gemini Configuration (Required if using Gemini as embedding provider)
GEMINI_API_KEY=your_gemini_api_key

# Embedding Provider Selection
# Options: "openai" or "gemini"
# Switch between providers by changing this value
EMBEDDING_PROVIDER=openai

# CORS Configuration
# For development: use "*"
# For production: comma-separated list of allowed origins
CORS_ORIGINS=*

# Environment
ENVIRONMENT=development

# Tesseract OCR Configuration (Optional)
# Only needed if tesseract is not in your system PATH
# Windows example: C:\Program Files\Tesseract-OCR\tesseract.exe
# Linux/Mac: usually not needed if installed via package manager
TESSERACT_CMD=
